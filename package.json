{
  "name": "password-timelock-crypto",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:security": "vitest run src/encryption/security.test.ts",
    "test:functional": "vitest run src/encryption/functional.test.ts",
    "test:benchmark": "vitest run src/encryption/benchmark.test.ts",
    "test:coverage": "vitest run --coverage",
    "test:save": "mkdir -p test-results && vitest run && cp test-results/results.json \"test-results/results-$(date +%Y%m%d-%H%M%S).json\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.16.1",
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "openpgp": "^6.2.2",
    "tlock-js": "^0.9.0",
    "zod": "^4.1.12"
  }
}
